<style>
.toolbar {
    width: 100%;
    background: #4A2C2A;
    position: fixed;
    top: 0;
    left: 0;
    z-index: 1000; /* Increased z-index to ensure it stays on top */
    box-shadow: 0 2px 8px rgba(74, 44, 42, 0.2);
}

.toolbar nav {
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem 1.5rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.logo {
    font-family: 'Playfair Display', serif;
    color: #E6C9A8;
    font-size: 1.8rem;
    font-weight: 700;
    letter-spacing: 1px;
    text-decoration: none;
}

.logo span {
    color: #D4A574;
}

.toolbar ul {
    display: flex;
    gap: 2.5rem;
    list-style: none;
    margin: 0;
    padding: 0;
}

.toolbar a {
    color: #E6C9A8;
    text-decoration: none;
    font-weight: 500;
    font-size: 1.1rem;
    transition: color 0.3s;
    position: relative;
}

.toolbar a:hover {
    color: #D4A574;
}

.toolbar a::after {
    content: '';
    position: absolute;
    width: 0;
    height: 2px;
    background: #D4A574;
    bottom: -4px;
    left: 0;
    transition: width 0.3s;
}

.toolbar a:hover::after {
    width: 100%;
}

.button {
    background-color: #D4A574;
    color: #2D1E1D !important;
    padding: 0.5rem 1.2rem;
    border-radius: 25px;
    font-weight: 600;
    transition: all 0.3s;
}

.button:hover {
    background-color: #E6C9A8;
    transform: translateY(-2px);
}

.button::after {
    display: none;
}

    @media (max-width: 768px) {
        .toolbar nav {
            flex-direction: column;
            gap: 1rem;
            padding: 0.8rem 1rem;
        }
        
        .toolbar ul {
            gap: 1.5rem;
            flex-wrap: wrap;
            justify-content: center;
        }
        
        .logo {
            margin-bottom: 0.5rem;
        }
    }
    
    @media (max-width: 576px) {
        .toolbar {
            position: absolute;
            background-color: rgba(74, 44, 42, 0.9);
            backdrop-filter: blur(10px);
        }
        
        .toolbar ul {
            gap: 1rem;
        }
        
        .toolbar a {
            font-size: 1rem;
        }
    }
</style>

<div class="toolbar">
    <nav>
        <a href="/" class="logo">Café<span>Trivia</span></a>
        <ul>
            <li><a href="#menu">Menú</a></li>
            <li><a href="#about">Nosotros</a></li>
            <li><a href="#gallery">Galería</a></li>
            <li><a href="#contact" class="button">Reservar</a></li>
        </ul>
    </nav>
</div>